<h1>ProjectOption list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Shortname</th>
            <th>Longname</th>
            <th>Description</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('projectoption_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.shortName }}</td>
            <td>{{ entity.longName }}</td>
            <td>{{ entity.description }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('projectoption_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('projectoption_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('projectoption_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
